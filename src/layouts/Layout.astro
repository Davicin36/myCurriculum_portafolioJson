---
interface Props {
	title: string;
}

/*estructura comun para las paginas de un sitio web, definimos aqui un diseño
general que se aplicara en cada pagina*/

const { title } = Astro.props;
---

<!doctype html>
<html lang="es">
	<head>
		
	</head>
	<body>
		<slot />
	</body>
</html>
<style is:global>
	html {
		font-family: Lucida,'Courier New', Courier, monospace;
		background: #ffff;
		letter_sapcing: -0.025rem;

	}

	body, figure{
		margin: 0;
		padding: 0;

	}

	a {
		text-decoration: none;
	}

	ul {
		list-style: none;
		margin: 0;
		padding: 0;
	}

	/* (*)asterorisco = selecciona todos los elementos de la pagina -->*/
	*, *::before, *::after{
		box-sizing: border-box;
	}
	
	h1, h2, h3, h4{
		margin: 0;
		font-family:system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;

	}

	p{
		color: #888;
		font-size: .8rem;
		line-heghit: 1.5;
		margin: 0;
		text-wrap: pretty;
	}

	
</style>
