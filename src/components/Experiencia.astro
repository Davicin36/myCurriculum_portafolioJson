---
import { work } from '@cv'

const {name, position, url, startDate, endDate} = work;

---

<section>
    <div>
        <div class="infoWork">
            <h1>{name}</h1>
            <h1>{position}</h1>
            <h1>{url}</h1>
            <h1>{startDate}</h1>
            <h1>{endDate}</h1>           

        </div>

    </div>

</section>